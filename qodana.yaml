####################################################################################################################
# WARNING: Do not store sensitive information in this file, as its contents will be included in the Qodana report. #
####################################################################################################################

version: "1.0"

exclude:
  - name: All
    paths:
      - node_modules
      - dist
      - build
      - .cloudflare

linter: jetbrains/qodana-js:latest

inspections:
  - name: ESLintValidationInspection
    enabled: false